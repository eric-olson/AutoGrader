<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :multipass_id %><br>
    <%= f.text_field :multipass_id %>
    <br>
    <%= f.label :theme %><br>
    <%= f.select :theme, UsersHelper.getValidAceThemesOptionArray %>
    <br>
    <%= f.label :keybind %><br>
    <%= f.select :keybind, UsersHelper.getValidAceKeybindingsOptionArray %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
