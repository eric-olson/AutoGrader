<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="form-group">
      <%= f.label :theme %><br>
      <%= f.select :theme, UsersHelper.getValidAceThemesOptionArray, {}, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :keybind %><br>
      <%= f.select :keybind, UsersHelper.getValidAceKeybindingsOptionArray, {}, class: "form-control" %>
    </div>
  </div>
  <div class="actions">
    <%= button_tag("Update", type: 'submit', class: "btn btn-default")%>
    <!-- <%= f.submit %> -->
  </div>
<% end %>
